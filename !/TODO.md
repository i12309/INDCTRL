# TODO

## Доступы

- [x] Отказаться от отдельной сущности `Role` и перейти на стандартные Django `Group`/`Permission`.
- [x] Перенести проверки web/API с `user.role.code` на группы или права (`user.has_perm(...)`).
- [x] Настроить пункты меню по конкретным правам, а не крупными блоками.

## Смены и сессии ESP32

- При повторном логине того же работника на том же станке продолжать активную `Work`, но выдавать новую `AuthSession`.
- Старую активную `AuthSession` для этой `Work` отключать или помечать замененной.
- Если активная смена принадлежит другому работнику, повторный login должен быть запрещен.
- Граница новой `Work` должна определяться графиком работы: если текущий login попадает в новый интервал графика, создавать новую смену; если в тот же интервал, продолжать существующую.
- Продумать обработку зависших смен после отключения питания: истечение TTL сессии не должно само по себе дробить смену, если график еще тот же.

## Допуск к станкам

- Убрать отдельную сущность `UserMachinePermission`; оставить только график как источник допуска работника к станку.
- Если в строке графика не заполнены дни недели, не ограничивать доступ по дням.
- Если в строке графика не заполнено время, не ограничивать доступ по времени.
- Поддержать варианты графика: только дни недели, только время, дни недели + время, без ограничений по дню и времени.
- Разрешить несколько строк графика для одного работника и станка с разными вариантами допуска.
